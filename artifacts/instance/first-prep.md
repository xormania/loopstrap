# first-prep.md — the named build queue (L34)

Everything Loopstrap has deliberately deferred, with its trigger. The engine's law
and machinery are complete (registers L1–L33, suite-proven); nothing below blocks
docs authoring, and nothing here is half-built — each item is unstarted by design.

## Trigger: a member's docs ratify (steward acts, trivial)
- **License-gate rewire** (L26/L28): launcher refusal reads the member's
  `contracts/<member>/docs-manifest.toml` verification instead of serena posture.
- **Posture flip**: member serena `read_only: false`, regen + replant.

## Trigger: first campaign prep (the three builds, each with tests)
- **Compiled-context generator** (L27): term cards · digests · anchors · scaffold
  from the ratified set; regenerated by the ripple; Agent Skills packaging.
- **Handoff dispatch tooling** (L12): bounded headless Claude invocations for the
  plan and plan-review handoffs; receipts filed in `campaigns/<id>/`.
- **Lane support** (L16): launcher `--lane`, `.worktrees/<lane>/` standup, per-lane
  locks; c1 defaults to one lane regardless.

## Trigger: c1 retro, from ledger data (measured decisions, never priors)
- Structural session-cycle enforcement in the launcher (L5).
- Per-role budget wiring on the D57 basis; Conductor model-tier review.

## Horizon (after the first member proves the system)
- `loopstrap-instantiate` (L15): mechanical instantiation of a fresh engine root.

## Owner one-timers (the box)
- Land the current courier at `~/projects/loopstrap` · member repo `main`+`dev` ·
  codex trust per repo · serena user/project precedence check at first codex run.

Integration friction from real docs lands on the **standard** (versioned, L28) —
docs-verify is the early warning; the engine holds still.
