# instance/ — the dev-lane governed instance (LIVE)

`process.md` governs campaign operations (v1, ratified pins P1–P15 + I1–I12). Companions per the tree: `fixtures/` (language-neutral corpus, exports at the c1 sweep) · `pins/` · `assets/` (dependency ledger · environment manifest · engineering crib · finder checklist — c2 prep builds) · `tools/` (preflight · PR-body generator — c2 prep builds). Family process law it binds: the fresh lsp_math registers under `../registers/`.

This file replaces the pre-c1 "to author before pass 1" stub — authored, landed, in force.
