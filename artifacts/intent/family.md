# Intent — family file (xor's voice; advisory, force-leveled)

Append-only; `lean:`/`rule:` entries, dated, one thought per line (see `README.md` here). xor edits this live — the one artifacts/ exception to the landing protocol. Empty is a first-class state: absence of intent never halts; it only means silences resolve conservative.
