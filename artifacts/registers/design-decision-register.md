# Loopstrap — Design Decision Register

Append-only; xor rules, entries never renumber, supersede by new entry. This is **Loopstrap's** register (L30): every entry is system law for the engine. Donor-era decisions remain with the donor's archive; deliverable projects (lsp_math first) get their own records only once their docs exist.

- **L1** *(2026-07-21, xor)* — The Loopstrap apparatus is fully adapted to lsp_math: no donor naming, corpus, or framing remains in lsp_math couriers, docs, or context. Courier/manifest/lock/root names carry the `lsp_math` token.
- **L2** *(2026-07-21, xor)* — The loop's integration line is `dev`: unit branches cut from `dev`, PRs target `dev`, merge on green judges; xor alone promotes dev→main. `main` is never the loop's. (Supersedes the carried donor `run/<n>/<cid>/int` pattern; the CI unit-lane gate scopes to `base.ref == dev`.)
- **L3** *(2026-07-21, xor)* — The lsp_math GitHub repo is `uscient/lsp_math`; the loop user on ubuntu-dev is `math-lsp` (P4 closed, `loop` lean overridden). Provisioned 2026-07-21 per transcript; evidence in `instance/assets/machines.md`.
- **L4** *(2026-07-21, xor)* — No Grok in the lsp_math environment: Grok Build (binary, PATH line, and its serena registration under `~/.grok/`) is removed from the `math-lsp` account; no lane exists for it in the apparatus.
- **L5** *(2026-07-21, xor)* — The loop conducts, never composes: content generation (code, tests, findings prose, retro drafts) rides role-prompted `codex exec` invocations; the loop keeps control acts only (plan, brief, dispatch, judge, disposition, custody, ledger, breaker, PR/merge). Deterministic judges run before the loop reads any diff; red re-dispatches mechanically with the judge tail. Briefs reference paths, never paste repo file bodies; tails before full logs. Session cycles at `units_per_session` = **10** (campaign.toml; Run-state table is the resume point); `generator_retry_cap` defaults to 3 (the standing three-passes rule). Per-invocation `role=` recorded (c1 baseline: `code`). The loop's model is **unchanged for c1** — retuning is a retro decision from role-split cost data.
- **L6** *(2026-07-22, xor — all five pins yes)* — Standing prep artifacts in `artifacts/guidance/`: a **segment map** (segments follow the ratified contract set's parties/layers; **required clause-coverage property** — every segment names the clauses it discharges and the union covers the set; dependency order is the backlog's spine) and **one guidance memo per segment** (real-world practice: candidate crates, reference implementations, failure modes, testing patterns — options + tradeoffs, dated + sourced, leans labeled). **Advisory force, firm:** guidance advises, contracts bind, judges gate; divergence is a PR-body line, never a gate. Drafted at Prepare, hash-cited at kickoff (§7 crib slot filled by these). The map derives from the ratified docs and cannot precede them.
- **L7** *(2026-07-22, xor — "trio is adopted")* — Per-unit trio topology, tests-first: **(t) test-writer** — inputs brief + verbatim clauses ONLY, never any diff/implementation; writes `tests/` only; clause-named fixture families → **loop vets tests** against the brief (scope check; invented requirements → one amended-brief test re-dispatch) → **(c) implementer** — brief + tests by path; adversarial tests are basis, disputes are Findings never edits → deterministic judges → **(r) reviewer** — one read-only invocation on the green diff, superseding the finder + advocate and inheriting both duties; findings contract: cited authority + location + severity, uncited ⇒ advisory; write-blocked — fixes ride a fresh implementer dispatch. Post-implement red is **arbitrated** by the loop, brief as authority: `test-bug` / `code-bug` / `spec-ambiguity` (footing rules). Per-role retry `generator_retry_cap`=3; **unit cap `unit_invocation_cap`=5** total role invocations, then `blocked` with the divergence as deliverable. `role=` values: `test`|`code`|`review` (`retro` at close). Sibling templates: `write-tests.md`, `review-code.md`; `implement-unit.md` header fixed (planner dissolved).
- **L8** *(2026-07-22, xor — "yes to all 4 pins")* — Operator record formats + one law: **(W1)** Run-state schema — fixed table columns (`unit|phase|inv|retries|branch|last_verdict|note`), phase enum `t-dispatched→t-vetted→c-dispatched→judged→arbitrating→reviewed→integrated` (+`blocked`), `COMPLETED` and `SESSION-CYCLE` line formats; the schema is the contract of resume. **(W2)** Every test-vet records `VET unit= clauses=n/m invented= verdict=`. **(W3)** Every arbitration lands `DIV-<n>` (judge · fixture · clause · attribution · evidence pointer) in the findings ledger, cited in the pass record. **(W4)** The test claims' **Intended API surface is unit basis at shape level** unless it contradicts a clause — then a Finding; never an edit. Addendum: xor granted Fable operational discretion within ruled law ("do what you want here — this your show"), with delegation-first standing for all production where feasible.
- **L9** *(2026-07-22, xor — "iterative process… recursive like")* — The split act completes the recursion: a unit approaching its invocation cap, accumulating ≥2 divergences, or carrying an untestably-wide clause set is split — parent terminal in phase `split`, children `<id>.<n>` each a full trio cycle with fresh caps, dependency-ordered. Invariants: **scope only narrows** (children's clause union ⊆ parent's; widening = backlog re-plan); **depth cap 3** (beyond ⇒ blocked → sweep: a spec/backlog defect, not decomposition). `SPLIT unit= into= reason=cap|divergence|scope at=` recorded; run-state phase enum gains `split`.
- **L10** *(2026-07-22, xor — "yes to all four")* — Decomposition moves to Prepare. **(a)** The backlog arrives unit-granular for the campaign in scope, rolling-wave (beyond the horizon: map-level only); entries carry clause set · size class · dependencies, coverage-checked against the segment map; the loop's planning narrows to sequencing within dependencies. **(b)** Sizing rule, written: one coherent fixture-family theme · one Intended-API seam · expected to land in the trio minimum with slack inside the cap. **(c)** Prep planning delegates: per in-scope segment, a `role=plan` codex invocation drafts a decomposition worksheet (map row + memo + clauses in; candidate units out) into `campaigns/<id>/`; steward curates; xor ratifies. `role=` gains prep-only `plan`. **(d)** L9 demotes to pressure valve: splits stay legal; every `SPLIT` is prima facie a prep miss — counted per campaign, sweep-reviewed, tuning the sizing rule.
- **L11** *(2026-07-22, xor — "I want Fable via claude code to do the planning")* — Planning authorship is **Fable's: the steward session (Claude Code) at the root**, at Prepare. The `role=plan` codex worksheet mechanism (L10c) is retired; the steward authors decomposition worksheets and the unit-granular backlog draft directly (mechanical enumeration may sub-dispatch; authorship and judgment are the steward's); xor ratifies. Prep acts are recorded in prep records, never the loop's pass ledger (the `plan` role value is removed from the loop's enum). The loop's part is unchanged: sequence ratified units; split only as the L9 valve.
- **L12** *(2026-07-22, xor — "delegate planning through a claude code handoff… i'm not directly involved… adversarial review foreach unit plan")* — Supersedes L11's inline-authorship detail. Planning is a **dispatched station**: per in-scope segment, one bounded, fresh-context **Claude Code plan handoff** authors the decomposition worksheet (paths in: map row · memo · clauses · sizing rule); one **adversarial plan-review handoff** (Claude Code, write-blocked, fresh context) attacks every unit plan — sizing-rule violations, coverage gaps/overlaps, dependency errors, invented scope, blind-testability — findings citing authority + location + severity, uncited ⇒ advisory; fixes ride a fresh planner dispatch, **cap 3 rounds per worksheet**, then route. The steward dispatches both and executes disposition. **xor holds no routine per-plan involvement**: backlog acceptance is implicit at ARM (kickoff cites the backlog hash); spec-ambiguity and unresolvable findings still route to xor — absence of routine involvement, never absence of authority. Dispatch tooling (bounded headless `claude`) is built at first prep. The plan lane thus mirrors the unit lane: author → blind adversary → write-blocked review → disposition, one level up.
- **L13** *(2026-07-22, Fable under granted discretion; xor's directive "minimize the circuit breaker going off")* — The arm-gate: a ten-point pre-ARM battery (`instance/arm-gate.md`) covering every historically-known preventable halt class — config drift, env walls, wall/audit state, codex trust, serena trust key + fleet warmth, auth + push probe, base-sha footing vs origin/dev, version drift (D38), budget posture, citation hashes. **ARM is legal only on a green gate**; the steward runs it and files the receipt in `campaigns/<id>/`. Minimization is achieved by prevention only — detection is never softened; any red is fixed, never waived. xor's routine surface, restated: ratify docs · standup acts (paste, codex trust, main+dev) · ARM · sweep + promote · optional budget caps.
- **L14** *(2026-07-22, Fable under discretion; evidence: xor — "vendor outage actually happened last test run, github had workflow action downages")* — CI-outage continue protocol, replacing halt-first: on detected Actions outage, record `CI-OUTAGE start=`, finish in-flight work locally (judges + reviewer are local), open PRs in phase `ci-deferred`, **never merge without CI green** (stricter than the admin-merge path it supersedes as the default), continue with dependency-schedulable units, halt-with-state only when nothing is schedulable; at recovery re-judge all deferred PRs and merge on green in dependency order, `CI-OUTAGE end=`. Owner admin-merge remains an option, never a need; promotion's hard gate untouched. Arm-gate gains item 11 (vendor status at ARM). Run-state phase enum gains `ci-deferred`.
- **L15** *(2026-07-22, xor)* — Frame: **Loopstrap is the product under development as a standalone system**; lsp_math is the proving instance (and a real deliverable xor will use). The system/instance seam is actively kept clean; L1 stands (this instance carries only lsp_math). Named future artifact: **`loopstrap-instantiate`** — mechanical instantiation of a fresh instance from the system core (the act performed by hand as the de-Exel port) — built after lsp_math proves the system.
- **L16** *(2026-07-22, Fable under discretion; xor: "abstracting the loop runner… to allow parallelism")* — Lanes: within-member parallelism. Prep emits lane assignments (L12 pipeline): backlog partitioned into lanes with disjoint clause sets AND disjoint expected file footprints; cross-lane dependency ⇒ same lane or an explicit sequencing gate. One loop session per lane in `.worktrees/<lane>/` (own `plan/`, own lock; session gate per-lane). Branch mechanics unchanged: all unit branches cut from `origin/dev`, PR into `dev`, merge on green — one integration line. Cross-lane merge conflict = **partition-miss** metric, sweep-reviewed (SPLIT's sibling). c1 defaults lanes=1; launcher lane support is first-prep tooling, built with tests; a 2-lane slice proves the architecture as soon as the segment map yields independent segments.
- **L17** *(2026-07-22, xor's articulation — "black-box cell units governed by contracts ⇒ parallel; recursion-styled breakdown" — landed by Fable under discretion)* — **The cell principle, recursive form**, named in §0: parallel-safe work at any scale (member · segment · lane · unit · split-child) is a contract-bounded black box — seam fully covered by ratified clauses at pinned hashes, interior invisible; parallelism is licensed exactly where the seam is covered; under-specified seams serialize or halt. L16's lane criterion re-ordered accordingly: **clause-seam disjointness is the partition criterion; footprint disjointness is a verified consequence** — clause-disjoint lanes with overlapping footprints are an architecture finding at plan review (module structure failing to mirror contract structure). Recursion validity = the scope-narrowing invariant preserving the parent's outer seam; within a unit, the Intended-API surface is the same seam.
- **L18** *(2026-07-22, xor — "get our terms straight… not conflating"; landed by Fable)* — The dev-lane working glossary, filed as `instance/terms.md`: stations pinned (Fable = class, not station; design session ≠ steward ≠ loop ≠ planner-handoff ≠ plan-review handoff; generator = mechanism, trio = roles), word discipline (invocation = codex in-run · handoff = Claude Code at prep · bare "review" banned), scales as recursive cells (split-child ⊂ unit ⊂ lane ⊂ segment ⊂ member ⊂ estate; campaign vs session), instruments disambiguated (kickoff/backlog/worksheet/brief; breaker/arm-gate/wall), and the vocabulary form of THE WALL. Scope wall explicit: terms.md is never the lsp_math lexicon and preempts none of its open rulings.
- **L19** *(2026-07-22, xor — "loop, let's call it the Conductor")* — The runner station is **the Conductor** (named for its L5 verb: conducts, never composes). This resolves lexicon Phase-0 C-3 **by claiming**: 'conductor' is released from the donor's runtime reservation and belongs to the dev lane. Wall **R1 retires** (its slot reserved for future product-lexicon terms); `conductor` joins the DEVMACH vocabulary guarded by R2/R5; the standing wall-allow exception retires with R1 (tests exercise the allow mechanism via `WALL_ALLOW` override). Legacy `loop` token survives only in mechanical identifiers (`launch-loop.sh`, locks, ledger fields, historic docs) and in xor's primer text, which is not edited — identifiers name artifacts, not the station.
- **L20** *(2026-07-22, xor — "Steward performs the smoke test… then launches conductor; conductor… delegating, and keeping track of the big picture")* — **Launch is a steward dispatch act**: legal only after (a) the green arm-gate receipt is filed and (b) xor's ARM; per lane under L16. Dispatch, not hands — the same class as the L12 prep handoffs. Sequence pinned: gate → receipt → request ARM → xor arms → steward launches. Altitude split pinned: the **Conductor's** big picture is its lane's run (delegation L5/L7, run-state L8); the **steward's** is the estate (board, cross-lane state, halt diagnosis). Terms: the verification battery's ruled name is the **arm-gate** ("smoke test" informal); post-L19 the cycle is **the run** — bare "loop" is retired from dev-lane prose entirely.
- **L21** *(2026-07-22, xor — "Steward IS me — it's my interface to the system")* — Identity ruling: the steward session is **xor's interface**, staffed by Fable, owned by xor — not a fourth agent. It explains the standing structure rather than changing it: **standing acts** (arm-gate, prep handoffs, dispositions, launch-after-ARM) execute without xor present — an interface executes standing orders (reconciles L12); **owner acts** are prepared to one pasteable line and never initiated — xor's literal hand executes, the owner record quotes his word. Capability walls unchanged and reframed as credential walls: the session's tools never write `contracts/`/`repos/`; promotion remains physically Box-2/xormania.
- **L22** *(2026-07-22, xor — "a mayday allows me to change anything — including authoritative docs")* — MAYDAY supremacy, written: under a declared MAYDAY, xor's authority is **total** — any artifact may change by his hand, including `contracts/` and the registers; no wall, protocol, or doctrine gates it; no agent records his MAYDAY changes as drift, breach, or finding (F-law yields in the moment). Stand-down = `walls on` + the **MAYDAY reconciliation**: recompute hashes of every changed authority; citing campaigns re-cite by kickoff amendment or halt for re-prep at their next hash check — mechanical consequence, never a gate on xor; the register records the delta, restoring append-only integrity by record. Mechanism fixes ridden along: takeover reach now `math-lsp` (was stale `loop`), usage line completed (panpan|mayday), prose post-L19.
- **L23** *(2026-07-22, xor — "mayday should also probably tell the conductor to pause (or give me option to)")* — **Pause**, distinct from halt: the breaker is the pause supervisor. `OWNER_PAUSE=1` on the forgery-proof override lane (honored only when the file-owner uid ≠ the runner's — sovereign runs from xor's account, never `math-lsp`) freezes the Conductor's process tree (SIGSTOP, parent-first; descendants walked via /proc), suspends the liveness wires, and keeps clocks honest; `OWNER_PAUSE=0` thaws (SIGCONT, parent-last) and resets stall/progress clocks — zero context loss, no breaker file, no re-arm. `sovereign.sh pause|resume` standalone; MAYDAY now asks **[p]ause / [h]alt / [w]alls-only** (default pause). Runner death while frozen falls through to normal drain/exit. Suite +2: the forgery-proof property itself is now a regression test (same-uid override ignored). Pause mechanics smoke-verified on a live tree (STOP→T, CONT→S).
- **L24** *(2026-07-22, xor — "what happens if I make a change that affects work in progress, or even completed work")* — Change-propagation law. **(a) Brief-atomicity:** a dispatched unit completes against its dispatched basis; amendments bind from the next dispatch; `invalidates:` ids in an amendment ⇒ named in-flight work abandoned (recorded, never integrated) and re-planned. **(b) The triad** is the mid-run change protocol: pause → change (MAYDAY if authoritative, L22) → reconcile → resume. **(c) The trace is canonical impact enumeration:** clause → segment map → backlog clause sets → merged units (trailers→briefs) → clause-named fixtures; requires stable clause ids + per-clause delta logs in the ratified docs (drafting requirement, filed in contracts/README). **(d) The ripple:** at next prep, or inside MAYDAY reconciliation, doc delta → affected completed units enumerated → the test role re-derives adversarial fixtures blind against the new clause text → green = survival evidenced, red = rework units enter the backlog via the plan pipeline. Completed work is never silently invalidated and never assumed valid; `main` lags by design — the ripple runs on `dev`.
- **L25** *(2026-07-22, xor — "Lexicon and System Contracts describe Loopstrap's deliverables — not Loopstrap")* — Document-class scope, pinned: the lexicon + system design contracts are **deliverable docs** — one set per member, describing what Loopstrap builds, serving as that member's launch key, governing the product lane only (R4 standing). Loopstrap the system is governed by its own dev-lane corpus (methods · instance · registers), evolving by register ruling. Two senses of "product," never conflated: Loopstrap is the product of the endeavor (L15); the member is the product of Loopstrap. A future formal spec of Loopstrap itself (the `loopstrap-instantiate` era) would be a new document about the system, not this class.
- **L26** *(2026-07-22, xor — "take my concepts of Lexicon and System Contracts (plus… a third doc) and make them easy to plug into Loopstrap")* — **The Deliverable Docs Standard** (`methods/deliverable-docs-standard.md`), Loopstrap's socket: per member, the **triad** — lexicon (meaning; first, authoritative) · system contracts (behavior; stable-id clauses) · **experience spec** (judgment — the third doc, answering the socket §7/§12 already cited; force: advisory-to-reviewer, taste informs / contracts gate) — plus **machine exports** generated from it: clause-index (party-tagged stable ids) · term-export (I1 judge) · parties (segment-map source) · **docs-manifest** (hashes + law lines + judge bindings; presence + verification = the member's license; launcher gate wiring = named first-prep tooling). Disciplines: stable ids + delta logs (L24), verbatim-quotability, the wall (R6, L25), lexicon-first authoring with a conflict pass. Open to xor's amendment like all method law.
- **L27** *(2026-07-22, xor's uploads — "don't take these as truth… if useful, let's use them")* — Advisory intake, dispositioned. **Method docs:** lexicon upload byte-identical to corpus; contract upload is an EARLIER draft (owner→authority, flip-condition→binding-trigger, companions and the design/run boundary absent→present in the tree's copy) — the corpus stands; the upload is archival signal of trajectory. **Living Project packet:** advisory orientation; its runtime-capability request is the far horizon of `loopstrap-instantiate`, parked. **Adopted now** into the Deliverable Docs Standard + process + terms: (a) **compiled context** as a required export class — anchors/term cards/digests/scaffold compiled from the ratified triad, regenerated on every amendment as part of the ripple, delivered verbatim (Agent Skills packaging = first-prep delivery tech); (b) **term-export in the method's own tiers** `BANNED|REVIEW|QUALIFY|CANON` — I1 consumes the lexicon's enforcement grammar directly; (c) **watched deferrals** — binding triggers reviewed by the steward at each Prepare; (d) terms cross-ref: the ripple is the absorption sweep's dev-lane form. **Open to xor:** the deliverable triad's third slot — (i) L26 stands (process system-side; experience third; lean, firm) vs (ii) per-member process rider.
- **L28** *(2026-07-22, xor — "i agree with your 3rd doc… I have to prepare these three docs, and need a format… a standard")* — The triad's third slot **ruled: the Experience Spec** (L27's open item closes; process stays system-side). The standard rewritten **interface-first (v2)**: methods evolve freely behind a fixed socket — Loopstrap binds file names, the clause anchor grammar (`[ID]` at line start, self-contained to the blank line), term tiers, the `[X-n]` experience anchor, the docs-manifest schema, and validation; never authoring method. **Compiled context reclassified as derived** — generated by Loopstrap at prep, never authored. Delivered: authoring templates (`methods/deliverable-docs-templates/`) and the gate itself — `instance/tools/docs-verify.sh` (hashes · clause bijection · party coverage · term tiers + lexicon presence · experience anchors · marker sweep · R6 wall) — suite-tested (conformant set passes; drift and unindexed clauses fail). Ratification = validator exit 0 + the owner's hand.
- **L29** *(2026-07-22, xor — "three prompts to use with my AI chat sessions to provide the docs in proper format")* — Authoring prompts shipped as system assets (`methods/deliverable-docs-prompts/`): one per triad doc, pasteable and self-contained, project-neutral (fill-in system line). Each carries the standard's hard grammar (anchors, self-containment, tiers, exports, marker rule), the load-bearing method disciplines for its doc type (prior-measurement, anti-definitions, subject-lock, acts-not-adjectives; decidability, failure doctrine, closure check; advisory force, rule-escape to contracts), the working relationship (session drafts, accountable authority rules), and the R6 prohibition phrased without enumerating dev-lane terms. Ordering enforced by inputs (2 requires the lexicon; 3 requires both); prompt 3 closes with the manifest + docs-verify ratification ritual.
- **L30** *(2026-07-23, xor, cross-chat — "everything to-date has been Loopstrap… I haven't ever been talking about Math LSP")* — **Scope recalibration.** One project has existed: Loopstrap. Every entry L1–L29, every parameter (cycle 10 · retry 3 · cap 5 · lanes), every vocabulary act (the Conductor claim) is Loopstrap system law — correctly ruled, previously mislabeled as lsp_math's; this register is retitled Loopstrap's (past entries untouched, append-only). **No lsp_math document exists**; prior-session draft material ("contract set v0.3," "Lexicon v0.1," O-rulings) is raw conversation input with **zero authority** — available paste-in for the authoring sessions, becoming lsp_math material only when xor authors it through the prompts into the ratified set. lsp_math begins the day its first doc does; the instance naming (L1) stands as the prepared home for that future member. Fable's "Phase-0 rulings" framing is withdrawn — the opener is prompt 1.
- **L31** *(2026-07-23, xor — "Go, yes" on the agnosticism audit)* — **The engine sheds the member's name.** System token `loopstrap` everywhere the engine speaks: courier `loopstrap-<UTC>.zip` · `loopstrap.manifest` · root `~/projects/loopstrap` · `loopstrap-loop.lock` · root-config + agent-config manifests + install markers · backup prefix · dashboard banner. **The member registry is born** — `artifacts/members.toml` (per member: repo, int branch); wall MEMBERS + per-member R3/R4 patterns, setup/reset clone legs, ops defaults, and the audit all derive from it; adding a member = one table + one staging dir, zero engine edits. **Judges-by-binding:** doctrine names "the member's bound judge set" (docs-manifest `[judges]` gains `profile`/`local`); cargo-four reframed as the shipped Rust profile — first language pack, no engine-wide language lock (member memory corrected). Docs homes go per-member: `contracts/<member>/`; docs-verify takes the member name. Residue swept: terms' product-words line, SYSTEM-MAP frame, machines rows, arm-gate per-member items, root docs, templates member-generic. Test fixture goes synthetic (`demo` member + fixture registry) — the suite now proves the engine, not a member. lsp_math persists purely as member config (registry row + staging + serena posture), per the ruled pin.
- **L32** *(2026-07-23, xor)* — Two standing clarities. **(a) The practical-example doctrine:** Loopstrap develops against a real member (lsp_math the proving instance, L15) — that is method, not error; the L31 registry seam is what makes it safe: member pressure shapes the engine through config, never through naming or coupling. **(b) Rust is the first-class default** member language — the shipped profile and the manifest default — because its toolchain (strict compiler · clippy · fmt · deterministic tests) is the strongest mechanical judge surface for agentic loops; the any-language socket stands unchanged for members that need it.
- **L33** *(2026-07-23, xor — "I want all the advantages of Rust for the Math LSP run — make sure we didn't lose that")* — Loss audit of L31's judge rebinding: one loss found and closed — the Rust profile existed as a name with no defining artifact (the flagged judge commands had dropped out of any authoritative home). **`artifacts/ci/profiles/rust.md`** now defines it: the four local judges verbatim with flags and order, the CI wiring (judges-rust.yml + cargo-deny + trailer/attribution + runtime-neutrality), and the provisioned promotable extras. The standard resolves profile names against `ci/profiles/`; docs-verify FAILS a manifest naming a missing profile; doctrine and process cite the profile file. Confirmed intact: judges-rust.yml content, deny.toml, the provisioned Rust stack on the runner account, lsp_math's serena language, and the L32 rust default — the member gets the full Rust advantage with zero extra authoring.
- **L34** *(2026-07-23, xor — "make sure any Loopstrap work is almost done")* — Completion posture, recorded: Loopstrap's law (L1–L33) and machinery are complete and suite-proven; all remaining work is deliberately deferred with named triggers, consolidated in `instance/first-prep.md` — two trivial steward acts at docs ratification, three tooling builds at first prep (compiled-context generator · handoff dispatch · lanes), two measured post-c1 decisions, one horizon item, and the owner's one-time box acts. xor's prediction is the design: integration friction from the three docs lands on the versioned standard via docs-verify, never on the engine.
- **L35** *(2026-07-23, xor — "abstract project config more from Loopstrap")* — **The derived-at-site discipline, named:** no deployment-derived value is ever hand-maintained as a literal in two places; one source computes it at the site, machinery detects divergence, humans grant. Wired: **(a)** the instance root is one knob — `LOOPSTRAP_ROOT` env (default `~/projects/loopstrap`) honored by setup and reset; **(b)** serena trust globs' source of truth is `ops/serena-fleet.sh`, which derives the pair from the actual root and three-way checks the live config — PASS / absent / **STALE** (present but wrong root — the exact failure the L31 move exposed) — printing the corrective line for the owner's hand, never writing global serena config itself; **(c)** the arm-gate and machines rows reference the derived check, carrying no path literals. The `__ROOT__` install-render (L-era) is recognized as the same pattern's first instance.
- **L36** *(2026-07-23, xor — "the steward… needs to be able to check current state and run any scripts needed")* — The interface gets its hands on the switches, not the wiring. **(a) Standing scripts allow-listed** (settings v4): status · install-configs full-run (deny lifted — the script's refuse-on-drift IS the wall) · wall · preflight · fleet · audit · docs-verify · tests · backup · custody · ops console · launch (post-ARM, L20); the scripts carry their own refusals — that is the wall model. **(b) Direct mutations stay Never**, and `rm` stays denied wholesale: owner one-liners (ARM foremost) run in xor's own shell pane, printed by the interface — L21's split made explicit. **(c) `ops/steward-status.sh`** born: one screen — tree-vs-manifest · config drift · wall · audit · per-member (staging/license/repo/branch tip) · breakers/paused/locks · board · the deferred queue; the first command of any steward session, charter-pointed.
- **L37** *(2026-07-23, xor — "no copy and pasting — Steward can do it")* — Owner acts execute **through the interface** at xor's explicit in-session word; the ask-gate's per-use click is his hand; the owner record quotes the word verbatim; the steward never initiates. Settings v5: `rm` and `Edit(contracts/ · registers/ · instance/)` denies lifted to the ask-gate — ARM and RATIFY become word→execute→click, zero paste. Unchanged deliberately: repos/.worktrees Edit denies, raw git/gh mutation denies, promote's Box-2 credential wall, sovereign's uid gate. Supersedes L36(b)'s print-and-paste split and refines L21's "prepared, never performed" into "performed at the word, never initiated."
- **L38** *(2026-07-23, xor — "the experience is I run claude code in the root directory and it knows the system")* — The wake protocol: on every steward session start, unprompted, the session runs `ops/steward-status.sh` itself (allow-listed), glances the board, the deferred queue, and any breaker/PAUSED state, and opens with a three-to-five-line sitrep before xor's first word. xor launches `claude` at the root and speaks — he runs nothing, pastes nothing (L37); the charter + SYSTEM-MAP + terms + register are the session's knowledge of the system. Supersedes any framing that hands xor a command ritual.
- **L39** *(2026-07-23, xor — "I only want it to warn me of potential issues from my directives — I'm not babysitting it. That's the WHOLE point of all this")* — The directive protocol: **xor's word executes; the steward warns, never seeks approval.** A foreseeable issue (ruled-law conflict · irreversible effect · state contradicting intent · red gate · stale base · budget) gets one concrete warning before execution; an issue-free directive executes silently with a result report; approval-seeking on clean directives is a named failure. Settings v6: owner-act surfaces (contracts · registers · instance · intent + the steward homes) and file ops allow-listed — no prompts; the repo-protection trade goes doctrinal (charter Never + owner records), accepted. The one physics exception stands: sovereign signals require xor's own uid (breaker forgery-proofing) and run from his pane, never through the runner-account session. Supersedes L37's ask-gate-as-hand.
- **L40** *(2026-07-23, xor — "i don't care if the system deletes the entire home dir by accident — it has its own user. as long as it's limited to that user")* — **The security model, stated: the user boundary is the security boundary.** Inside `math-lsp`, everything is expendable by declaration; settings v7 empties the deny list entirely — any permission mode, `--dangerously-skip-permissions` included. Exactly two assets escape the user and keep their guards where they live: **origin/main** (server-side protection, Box-2 promote — a GitHub fact) and **spend** (the breaker's uid-forgery-proof override lane — the sovereign uid-check was always the wallet's guard, nothing else's). All former in-session walls are reclassified: damage-motivated → deleted; integrity-motivated (steward never hand-edits member source or pushes) → doctrine, existing for record-truth (attribution · judge evidence · sweep meaning), detected via git history + owner-records, never prevented. L39's warn-then-execute stands over everything.
- **L41** *(2026-07-23, xor — "what other security overkill have you baked into this?")* — Full inventory dispositioned. Remaining mechanisms classified: **real security** = spend wires + uid-gate and the main/dev server-side facts, nothing else; **trust substrate** = judges, attribution, walls, hashing, custody, spec-less refusal — record-truth that replaces watching, not gates on xor; **integrity plumbing** = courier/land/install/audit/status, zero-friction; **vendor one-timers** = codex trust, git identity. One residual babysitting item found and removed: default-mode per-use prompts in the steward session — settings v8 sets `defaultMode: bypassPermissions`; `claude` at the root is prompt-free with no flags.
- **L42** *(2026-07-23, xor — "I can't have the steward fighting me — do panpan and mayday still make sense or need to be defined better?")* — **The two-channel doctrine.** Channel 1, conversation: xor's word is sovereign at the steward — the "regardless of any word" class is abolished; every directive is warn-once → comply → record; the only surviving refusals are physics (promote's absent credential; the breaker's uid lane, the wallet's spine). Channel 2, signals: PAN-PAN / pause / MAYDAY are the out-of-band channel to the **headless** layer — the flying Conductor + breaker, which cannot hear words — from xor's own uid by forgery-proof necessity. MAYDAY sharpened: its walls-off leg retargets the **member-planted** session guards (root carries none since L40), backed up beside each file and restored byte-exact at stand-down; pause/halt choice, total authority (L22), and reconciliation unchanged. The signals make sense precisely as the flight channel; the steward never needed one.
- **L43** *(2026-07-23, xor — "ARM is our version of Loopstrap system's test — correct? We need better testing… see deterministically the repercussions")* — Terminology corrected: ARM = owner act; arm-gate = one instance's readiness at one moment; **the suite is Loopstrap's test**. Better testing landed: **`tests/battery.sh`** — suite + wall sweep + audit, one command, one exit code, the repercussion detector after any change (the per-seal ritual, now dogfooded); new cases closing today's own gaps — sovereign walls mechanics (empty/backup/byte-restore, shipped untested hours earlier), launcher refusal paths (breaker present · live lock), the L33 profile-binding FAIL, status render; **`tests/COVERAGE.md`** maps component→case and names the deliberately-untested residue so blind spots are visible, never silent.
- **L44** *(2026-07-23, xor — "you're creating the tests that you then run — see the issue there?")* — Named and bounded: author-written tests prove conformance to the author's model; author-model blindness has no self-administered cure (the flock-vs-PID case is the same-day demonstration; so is the coverage table citing a case file that never existed). Landed mitigations, honestly labeled: **register-citation headers** on every case (test selection auditable against xor's rulings); **`tests/mutation-check.sh`** — deliberate guard-breaks on verbatim-held lines, each must turn the suite RED, tree restored byte-exact (kills vacuous tests); **Independence limits** in COVERAGE.md ranking the layers and naming the real cures: xor's read, a fresh-context adversarial suite review (his to order), the horizon engine doc set as a blind basis. The battery stays the change detector; the mutation check is the detector's detector.
- **L45** *(2026-07-23, xor — commissioned external scan of the testing system; findings intaken per the standing protocol)* — **Scan dispositions, all landed same-day; zero disputes.** The scan's verdict — L43 partially supported, L44's mutation-check unsound — was **reproduced finding-for-finding before repair** (red-baseline mutation "ok"s; SKIP-as-success; zero-test/aborted-case/empty-case green; battery green under dead `ops/land.sh`+installer, `default` exec mode, and an unswept R5 breach; contaminated bijection fixture; base==main SHA blindness; 41-of-71 sweep). Repairs: **harness** — zero matched cases, zero-assertion cases, and abnormal termination each FATAL; **mutation-check v2** — green-battery baseline precondition (abort 8), isolated copies, unapplied-mutation FATAL, expected-failure attribution per named leg, the legs themselves mutation targets, live tree re-verified untouched; **real landing + installer cases** (ops/land.sh convergence/stale-removal/preservation/refusals; installer plant/render/idempotence/drift/refusal) replacing the stub-under-a-claiming-header; **full-path** re-fixtured base≠main and asserted on the SHA; **docs-standard** negatives each on an independent true-hash fixture with attributed failures; **sweep = full corpus** (`artifacts/**/*.md` + root `*.md`; `contracts/README.md` scope line reworded — it tripped R6 with the tokens it prohibits; R6 itself unweakened); **audit** exec-line mode check made semantic; **breaker** tokens-out wire case added; **REGISTER-MAP.tsv + checker as battery leg 4** — assertion-level citations checked every run, superseding prose headers. **Recorded-unverified, named in COVERAGE residue:** `max_budget_usd` enforcement point, preflight refusal matrix, stall/drain/PAN-PAN live wires, orphan/`ci-deferred` reconciliation. Standing act adopted: periodic fresh-context adversarial review — the one layer that caught what self-administered layers could not.
- **L46** *(2026-07-23, xor — second commissioned review, whole-project scope; verdict REJECT)* — **L45 overclaimed; the correction is recorded here rather than by amending it.** The repairs L45 describes were real, but three of its words were not earned: "semantic" named a narrower grep that a decoy assignment defeated; "attribution" named a substring search that a *passing* assertion's own label satisfied; "zero disputes" described an intake and was read as a soundness result. That is the same defect this project keeps finding elsewhere — printed intent standing in for state — appearing in the entry that claimed to have fixed it. **Repaired this round, each reproduced first:** the harness now fails a case that exits early with status 0 (completion marker written inside the case subshell), fails a case that shadows an assertion helper (four definitions hashed either side of every case, the failure counted without routing through the accused helper), and exits 1 rather than a raw count that wrapped mod 256 at 256 failures; every assertion is recorded at runtime and the map leg reconciles **declared against executed**, so a commented-out or unreached assertion is a hole rather than a silent pass; mutation-check v3 requires a **causal transition** — the named assertion PASS in baseline, not PASS under the mutant — over eighteen mutants covering the guards, all five legs, the harness and the map; the launcher check requires exactly one exec assignment and reads its mode; a **syntax leg** was added and immediately found a shipped operator script that had never parsed, plus an unbound-`$ROOT` ordering bug in two of them. **Not repaired, and now named as known-broken rather than untested** in COVERAGE: landing's false `LANDED CLEAN` over a stale directory, the installer's success on an absent member repo, the sovereign→breaker `override.env` path mismatch that disconnects live owner control in production, MAYDAY's root-guard path, the docs verifier licensing malformed sets through a lost pipeline-subshell counter, custody trace overwrite, permissive vendor mocks, the reset boundary, wall edges, fixed temp paths, unconditional banners. **Standing rule adopted:** a register entry may not assert a property the same-day receipts do not show; where a check is lexical it is called lexical. Four items are xor's to rule on, not engineering: manifest exhaustiveness, absent-member-repo policy, whether the ops surfaces are load-bearing truth or presentation, and the admissible `LOOPSTRAP_ROOT` boundary.
- **L47** *(2026-07-23, xor — "ok - can you fix it for me")* — **Repair authorization for L46's demonstrated engineering defects.** This is not treated as a new product-doctrine ruling or as retroactive approval of L45/L46 prose. The implementation uses conservative, explicit operational semantics where L46 exposed ambiguity: the courier's static inventory is exhaustive and mode-sealed while named runtime custody paths remain mutable; every registered member checkout is required state; operator status is truth-bearing and read-only; reset accepts only a non-symlink descendant of the dedicated account HOME. The other reproduced defects are repaired at their existing seams: transactional landing; schema-complete docs validation; immutable custody snapshots; canonical sovereign/breaker override path plus campaign-scoped stop; strict vendor mocks; parsed and scoped wall inputs; canonical token/status accounting; read-only audit; private harness evidence; dispatcher receipts; and isolated mutation witnesses. The receipts are `tests/battery.sh`, `tests/mutation-check.sh`, `tests/REGISTER-MAP.tsv`, and `tests/COVERAGE.md`; their claims remain bounded by what those receipts actually execute.
- **L48** *(2026-08-07, xor — "you can't shoot yourself in the foot - not in my repo's and not on my time" · "it has to impossible for it to not have a path through" · "that is what i want in the CI")* — **Every control has a path from red to green.** Not merely that some good input passes; that from a RED state the documented remedy REACHES green. A control without such a path is not strict, it is broken, and it is worse than no control: the only way past it is to disable it, and the first bypass retires it permanently while leaving it in the tree looking alive. A prior effort lost roughly twenty hours to this class. It has shipped here three times — `acceptance-check` with an empty intersection between its CUE and Python required-field sets (REVISION-008); the CI body check, which could not be re-run after the body was edited; and `publication-check.py`, which refused any edit to a line already carrying a term. Each pairing in `tests/cases/controls-reachable.sh` forces red, applies the remedy, and asserts green, so a control that admitted everything fails the red half rather than passing silently. Enforced in the battery, and the battery is CI.
