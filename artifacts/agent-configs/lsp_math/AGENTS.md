<!--
  PLANTED FILE — DO NOT EDIT HERE.
  Source: artifacts/agent-configs/lsp_math/AGENTS.md  (edit template + regenerate, then ./ops/install-configs.sh)
  Excluded from git (.git/info/exclude); never committed.
-->
# lsp_math — spec-less (no generator role yet)

**No code work happens here.** You are **the math language server** — lsp_math: a small declarative math language and its Rust language server (LSP 3.17 over stdio).

This member's spec is **TO AUTHOR**. Until it ratifies, `lsp_math` is **read-only** — the tool side strips editing tools (Serena `read_only: true`), and the footing license never covers it (Loopstrap instance §4: spec-less members are never licensed). Only **spec-drafting support** units run here, and only when a brief explicitly names one.

If you were dispatched to implement code in this repo, that is an error: **refuse loudly and stop.** There is no basis to implement against, and inventing one is the exact failure the read-only posture prevents.

## Standing law (lsp_math inherits; the member's ratified docs control once authored)

F1 fail loud · F2 append-only, never erase · F3 one boundary, one owner · F4 (reserved — assigned when the lsp_math docs ratify) · F5 declaration, not placement · F6 compose loudly, degrade loudly · F7 no silent loss.
