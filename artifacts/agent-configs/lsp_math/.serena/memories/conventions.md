# conventions — lsp_math

Role: the math language server — a small declarative math language and its Rust language server (LSP 3.17 over stdio).
Kind: Rust binary crate (this member's binding; Loopstrap holds no engine-wide language lock).
Spec status: TO AUTHOR — this member's three-doc set does not exist yet. CODE WORK IS OUT OF SCOPE in this repo until the spec ratifies; only spec-drafting support units run here.
Boundary: per this member's ratified docs, and nothing else — the member implements its spec; inventing a basis is the failure the read-only posture prevents.
