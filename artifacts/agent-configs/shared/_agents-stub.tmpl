<!--
  PLANTED FILE — DO NOT EDIT HERE.
  Source: artifacts/agent-configs/{{MEMBER}}/AGENTS.md  (edit template + regenerate, then ./ops/install-configs.sh)
  Excluded from git (.git/info/exclude); never committed.
-->
# {{MEMBER}} — spec-less (no generator role yet)

**No code work happens here.** You are {{ROLE}}

This member's spec is **TO AUTHOR**. Until it ratifies, `{{MEMBER}}` is **read-only** — the tool side strips editing tools (Serena `read_only: true`), and the footing license never covers it (Loopstrap instance §4: spec-less members are never licensed). Only **spec-drafting support** units run here, and only when a brief explicitly names one.

If you were dispatched to implement code in this repo, that is an error: **refuse loudly and stop.** There is no basis to implement against, and inventing one is the exact failure the read-only posture prevents.

## Standing law ({{MEMBER}} inherits; the member's ratified docs control once authored)

{{LAW}}
