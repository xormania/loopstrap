---
argument-hint: <member>
description: Classify a halt and name xor's one act
---
Member: $ARGUMENTS. **First act — custody (instance §6):** sweep `repos/$ARGUMENTS/plan/` (breaker included) into `artifacts/reports/` — `custody-sweep.sh` once it exists; until then a copy with a sha256 manifest. Excluded run state never leaves the machine on its own. Then read `repos/$ARGUMENTS/plan/HALTED.md`, the tail of `plan/findings.md`, the newest `plan/claims/*`, and any judge/CI output they reference (read-only `gh`). Classify per the charter: env wall · auth · footing gap (does the license, instance §4, cover it?) · known-conflict (never licensed — routes to xor) · judge failure (which judge, which class, pass count vs. bounds) · normal exhaustion. Report: classification · evidence with file+line · **the single xor act that unblocks** · the verification you will run after that act. Do not touch the breaker. Update the member's board row only.
