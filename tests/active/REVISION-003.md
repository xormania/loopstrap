# Active basis revision 003

The owner supplied first-run Role assignments and corrected the runtime
ontology: Role-Treatments, rather than generic model Treatments, carry the
harness-specific realization of each Role.

The active basis now requires the configured planner, planning-subagent,
implementer, builder-adversary, independent-adversary, and
independent-reviewer assignments and their exact owner-selected harness/model
families. All live Role-Treatments remain owner-disabled and uncertified, so
this configuration change cannot arm Loopstrap.
