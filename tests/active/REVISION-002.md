# Active basis revision 002

The `ACTIVE-09` battery-wiring witness originally required distinct core and
active receipts. Harness certification is now a separately frozen acceptance
surface, so the same witness also requires its independent dispatch and receipt.

This extends the battery-causality check without weakening or replacing the
original boundary. The harness-certification checkpoint preserves the earlier
frozen active basis.
