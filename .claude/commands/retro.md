---
argument-hint: <campaign-id>
description: Assemble a retrospective skeleton from run artifacts
---
Campaign: $ARGUMENTS. From the member's `plan/` (findings ledger, claims, unit briefs, backlog) plus PR bodies and CI verdicts via read-only `gh`, assemble `artifacts/campaigns/$ARGUMENTS/retrospective.md` on the standing shape (instance §3.4): facts table · the minted DEFAULT surface with family-wide vs. member-local per row · lessons grouped (blockers / consistency / efficiency / quality gaps judges can't see / what worked) · a sweep-input list: every DEFAULT and F-* with a proposed route (ratify / rule / fix / defer) and a stated lean. Draft only — dispositions resolve by xor's numbers at the sweep (instance §3.5).
