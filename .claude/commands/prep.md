---
argument-hint: <campaign-id>
description: Phase-1 checklist for arming a campaign
---
Campaign: $ARGUMENTS. Run the phase-1 checklist (instance §3.1): machine provisioned — `provision.sh` verified against the environment manifest (FR-005; unprovisioned means NOT armable); staging drift-free (`./install-configs.sh --check`); `artifacts/campaigns/$ARGUMENTS/` holds kickoff + rider + curated backlog; every cited input exists with its sha256 recorded (registers, the member spec from the ratified lsp_math docs, shared assets incl. environment manifest, brief template); judge set per instance §10 present in the member's CI **as workflow files** (server-side requiredness is xor's fleet-row act, never asserted here; I1 vocabulary judge is dependency-blocked on the lsp_math lexicon — report as pending-dependency); codex repo trust recorded for the member; no other live session on it (rule 8); serena CLI + fleet context healthy; model/effort/reasoning-summary resolve to the manifest's pins; open register items that gate this member named; known-conflict scan — any two ratified texts colliding on this member means NOT armable (instance §4). Output: **READY** or the blocking list, each item with the act and owner that clears it. Arming stays xor's act; never touch the breaker.
